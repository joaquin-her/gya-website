
1. Para crear una imagen y levantar el contenedor con el Dockerfile: ´docker compose up --build´ en el root del repositorio.
*Asegurarse de que docker esta como servicio corriendo de fondo en el SO paa cada comando a ejecutar*

2. Con 'docker ps -a' se pueden enlistar los contenedores ejecutandose y los que estan creados.
